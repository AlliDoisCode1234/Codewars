const last = x =>
//   p: string
//   R: array of sorted strings by last letter
  
//   P: split words into array,
    // sort by last letter
  
  
  x
    .split(' ')
    .sort((a,b) => b[b.length -1] < a[a.length -1])
  
  
   
